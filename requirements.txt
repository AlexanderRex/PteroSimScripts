# Core numeric/RL dependencies used by run_rl_race_train.py
numpy
gymnasium
stable-baselines3
torch
tensorboard

# PteroSim SDK runtime dependencies (from the main PteroSim repository)
grpcio>=1.60.0
protobuf>=4.25.0

# To interact with the simulator API, download and install the PteroSim SDK.
